// Shared between MoneyMadeCard and InvestmentSummaryCard so their bottoms
// always land on the same line in Finance.jsx's Grid row, independent of
// how many employees are in the Money Made list (its table scrolls inside
// this height rather than growing the card).
export const FINANCE_ROW_HEIGHT = 400;
