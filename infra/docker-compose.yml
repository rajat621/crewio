version: '3.8'

services:
  # MongoDB Replica Set (single node for dev)
  mongodb:
    image: mongo:7.0
    container_name: crewio-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: crewio
    volumes:
      - mongo_data:/data/db
      - mongo_config:/data/configdb
    command: --replSet rs0 --auth
    networks:
      - crewio-network
    healthcheck:
      test: echo "db.runCommand('ping').ok" | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5

  # Initialize MongoDB replica set
  mongo-init:
    image: mongo:7.0
    container_name: mongo-init
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - crewio-network
    command: >
      mongosh --host mongodb:27017 -u admin -p password --authenticationDatabase admin --eval
      "rs.initiate({_id:'rs0',members:[{_id:0,host:'mongodb:27017'}]});"
    restart: on-failure

  # Redis (optional, for pub/sub scaling)
  redis:
    image: redis:7-alpine
    container_name: crewio-redis
    ports:
      - "6379:6379"
    networks:
      - crewio-network
    volumes:
      - redis_data:/data

  # Backend NestJS application
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    container_name: crewio-backend
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: development
      MONGODB_URI: mongodb://admin:password@mongodb:27017/crewio?authSource=admin&replicaSet=rs0
      JWT_SECRET: dev-secret-key-change-in-production
      JWT_EXPIRATION: 3600
      REDIS_URL: redis://redis:6379
      CORS_ORIGIN: http://localhost:5000,http://localhost:8080,http://localhost:5173
    depends_on:
      mongo-init:
        condition: service_completed_successfully
    volumes:
      - ./backend/src:/app/src
      - ./backend/node_modules:/app/node_modules
    networks:
      - crewio-network
    command: npm run start:dev

volumes:
  mongo_data:
  mongo_config:
  redis_data:

networks:
  crewio-network:
    driver: bridge
