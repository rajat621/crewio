# This file is superseded - see `.env.example` (with the leading dot) in
# this same directory for the current, reconciled list of environment
# variables. This file previously diverged from `.env.example` (missing
# AI_SERVICE_SHARED_SECRET/APP_ENV, the two most security-critical
# variables) - kept as a pointer rather than deleted so anything still
# referencing this filename by name doesn't just silently 404.
